
 model_list:
   # Claude-4 Haiku (Hızlı ve ekonomik - %40 kullanım)
   - model_name: autox
     litellm_params:
       model: anthropic/claude-haiku-4-5-20251001
-      api_key: os.environ/ANTHROPIC_KEY_HAIKU
+      api_key: os.environ/ANTHROPIC_KEY_HAIKU
     max_parallel_requests: 8
-    weight: 40
     tpm: 50000  # tokens per minute
     rpm: 100    # requests per minute
+  - model_name: autox
+    litellm_params:
+      model: anthropic/claude-haiku-4-5-20251001
+      api_key: os.environ/ANTHROPIC_KEY_SONNET
+    max_parallel_requests: 8
+    tpm: 50000
+    rpm: 100
+  - model_name: autox
+    litellm_params:
+      model: anthropic/claude-haiku-4-5-20251001
+      api_key: os.environ/ANTHROPIC_KEY_SONNETX
+    max_parallel_requests: 8
+    tpm: 50000
+    rpm: 100
 
   # Claude-4 Sonnet (Premium - %30 kullanım)
   - model_name: sonnet-4-x
     litellm_params:
       model: anthropic/claude-sonnet-4-20250514
-      api_key: os.environ/ANTHROPIC_KEY_SONNETX
+      api_key: os.environ/ANTHROPIC_KEY_HAIKU
     max_parallel_requests: 4
-    weight: 30
     tpm: 30000
     rpm: 60
+  - model_name: sonnet-4-x
+    litellm_params:
+      model: anthropic/claude-sonnet-4-20250514
+      api_key: os.environ/ANTHROPIC_KEY_SONNET
+    max_parallel_requests: 4
+    tpm: 30000
+    rpm: 60
+  - model_name: sonnet-4-x
+    litellm_params:
+      model: anthropic/claude-sonnet-4-20250514
+      api_key: os.environ/ANTHROPIC_KEY_SONNETX
+    max_parallel_requests: 4
+    tpm: 30000
+    rpm: 60
 
   # Claude-4.5 Sonnet (En yeni - %30 kullanım)
   - model_name: sonnet-4-5-x
     litellm_params:
       model: anthropic/claude-sonnet-4-5-20250929
-      api_key: os.environ/ANTHROPIC_KEY_SONNET
+      api_key: os.environ/ANTHROPIC_KEY_HAIKU
     max_parallel_requests: 4
-    weight: 30
     tpm: 30000
     rpm: 60
+  - model_name: sonnet-4-5-x
+    litellm_params:
+      model: anthropic/claude-sonnet-4-5-20250929
+      api_key: os.environ/ANTHROPIC_KEY_SONNET
+    max_parallel_requests: 4
+    tpm: 30000
+    rpm: 60
+  - model_name: sonnet-4-5-x
+    litellm_params:
+      model: anthropic/claude-sonnet-4-5-20250929
+      api_key: os.environ/ANTHROPIC_KEY_SONNETX
+    max_parallel_requests: 4
+    tpm: 30000
+    rpm: 60
@@
 rate_limiting:
@@
   model_rate_limits:
     autox:
-      global_tpm: 400000  # 8 paralel * 50K
-      global_rpm: 800     # 8 paralel * 100
+      global_tpm: 1200000  # 3 deployments * (8 * 50K)
+      global_rpm: 2400     # 3 deployments * (8 * 100)
     sonnet-4-x:
-      global_tpm: 240000  # 8 paralel * 30K (2 instance)
-      global_rpm: 480     # 8 paralel * 60
+      global_tpm: 360000   # 3 deployments * (4 * 30K)
+      global_rpm: 720      # 3 deployments * (4 * 60)
     sonnet-4-5-x:
-      global_tpm: 240000
-      global_rpm: 480
+      global_tpm: 360000
+      global_rpm: 720
